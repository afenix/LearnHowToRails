<%= render "layouts/errors", :object => @lesson %>

<%= bootstap_form_for(@lesson) do |f|%>
  <%= f.text_field :title %>
  <%= f.text_area :body, rows: 15 %>
  <%= f.submit class: 'btn btn-primary'%>
<% end %>
